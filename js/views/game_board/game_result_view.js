// Generated by CoffeeScript 1.7.1
(function() {
  var _base,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.Movie || (window.Movie = {});

  (_base = window.Movie).Views || (_base.Views = {});

  Movie.Views.GameResultView = (function(_super) {
    __extends(GameResultView, _super);

    function GameResultView() {
      return GameResultView.__super__.constructor.apply(this, arguments);
    }

    GameResultView.prototype.template = _.template($("#game_result_template").html());

    GameResultView.prototype.el = ".container";

    GameResultView.prototype.initialize = function() {
      if (this.attributes.players === "single") {
        this.singlePlayerStats();
      }
      if (this.attributes.players === "multiplayer") {
        return this.multiPlayerResults();
      }
    };

    GameResultView.prototype.singlePlayerStats = function() {
      this.$el.html(this.template());
      this.$('#player-name').html(this.attributes.playerName);
      this.$('#single-player-stats').html("You got " + this.attributes.correctAnswers + " out of " + this.attributes.totalQuestions + " questions right!");
      return this.render();
    };

    GameResultView.prototype.multiPlayerResults = function() {
      this.$el.html(this.template());
      this.$('#player-name').html(this.attributes.playerName);
      this.$('#single-player-stats').html("You got " + this.attributes.correctAnswers + " out of " + this.attributes.totalQuestions + " questions right!");
      return this.render();
    };

    GameResultView.prototype.render = function() {
      return this;
    };

    return GameResultView;

  })(Backbone.View);

}).call(this);
